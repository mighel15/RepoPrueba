package aceituno.rojo.photomath.adapters

import aceituno.rojo.photomath.R
import android.os.Bundle
import android.view.LayoutInflater
import android.view.ViewGroup
import android.widget.TextView
import androidx.navigation.findNavController

class ProfesionalsAdapter {//: RecyclerView.Adapter<ProfesionalsAdapter.ViewHolder>() {
    /*override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder {
        val view = LayoutInflater.from(parent.context).inflate(R.layout.item_rv_personas, parent, false)
        return ViewHolder(view)
    }

    override fun getItemCount(): Int {
        return 0
    }

    override fun onBindViewHolder(holder: ViewHolder, position: Int) {
        //holder.setValues(lst[position])
        holder.itemView.setOnClickListener {

        }
    }

    class ViewHolder(view:ViewHolder) : RecyclerView.ViewHolder(view) {
        private val tvTituloReto = view.findViewById(R.id.tvTituloReto) as TextView
        private val tvUsuario = view.findViewById(R.id.tvUsuario) as TextView

        fun setValues(model: RetoPorAreaResponse){
            tvTituloReto.text = model.nombre
            tvUsuario.text = model.usuario
        }
    }*/
}